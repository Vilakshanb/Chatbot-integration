def format_response(data):
    # This is a placeholder. You'd need to format the data based on its structure.
    # For demonstration purposes, let's assume the data is a dictionary with a 'returns' key.
    return f"Your portfolio returns are: {data.get('returns', 'N/A')}"
